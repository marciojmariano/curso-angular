export interface ClienteRequest{
    nome: string;
    telefone: string;
    credito: number;
}

export interface ClienteResponse{
    id: number;
    nome: string;
    telefone: string;
    credito: number;
}